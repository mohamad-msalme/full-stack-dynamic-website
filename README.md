### To run locally

1. Enter the client app directory then run:

   ```sh
   npm run dev
   ```

2. Enter the server app directory then run:

   ```sh
   npm run watch
   ```

   ```sh
   npm run dev
   ```

3. Front end port: <http://localhost:5173>
4. Backend port: <http://localhost:3002>
